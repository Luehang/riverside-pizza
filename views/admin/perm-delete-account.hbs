<div class="row">
    <div class="col-md-8 col-md-offset-2">
        <form action="/admin/user-accounts/delete/perm/user?_method=DELETE" method="POST">
            <input type="hidden" name="_csrf" value="{{ csrfToken }}">
            <h3>Permanently Deleting Account {{userEmail}}...</h3>
            <hr>
            {{#if hasErrors}}
                <div class="alert alert-danger">
                    {{# each messages }}
                        <p>{{this}}</p>
                    {{/each}}
                </div>
            {{/if}}
            <div class="alert alert-warning">
                <p>Deleting an account permanently will erase part of the order history and might break other parts of the website content.  Do with caution.</p>
            </div>
            <p class="description">Please enter admin. email and password to confirm user's deletion or cancel.</p>
            <div class="form-group">
                <label for="email">Admin. E-Mail</label>
                <input placeholder="name@example.com" id="email" name="email" class="form-control" autocomplete="email">
            </div>
            <div class="form-group">
                <label for="password">Admin. Password</label>
                <input type="password" id="password" name="password" class="form-control">
            </div>
            <div class="form-group">
                <label for="email">User's E-Mail</label>
                <input placeholder="name@example.com" id="email" name="userEmail" class="form-control">
            </div>
            <div class="form-group">
                <label for="email">Confirm User's E-Mail</label>
                <input placeholder="name@example.com" id="email" name="userEmailConfirmation" class="form-control">
            </div>
            <button type="submit" class="btn btn-success">CONFIRM</button>
            <button type="submit" class="btn btn-danger" formaction="/admin/user-accounts" formmethod="GET">CANCEL</button>
        </form>
    </div>
</div>