<div class="row">
    <div class="col-md-8 col-md-offset-2">
        <form action="/admin/user-accounts/delete/temp?_method=DELETE" method="POST">
            <input type="hidden" name="_csrf" value="{{ csrfToken }}">
            <h3>Temporarily Deleting Account {{userEmail}}...</h3>
            <hr>
            {{#if hasErrors}}
                <div class="alert alert-danger">
                    {{# each messages }}
                        <p>{{this}}</p>
                    {{/each}}
                </div>
            {{/if}}
            <p class="description">Please enter admin. email and password to confirm user's deletion or cancel.</p>
            <div class="form-group">
                <label for="email">Admin. E-Mail</label>
                <input placeholder="name@example.com" type="email" id="email" name="email" class="form-control" autocomplete="email" required>
            </div>
            <div class="form-group">
                <label for="password">Admin. Password</label>
                <input type="password" id="password" name="password" class="form-control">
            </div>
            <div class="form-group">
                <label for="email">User's E-Mail</label>
                <input placeholder="name@example.com" type="email" id="email" name="userEmail" class="form-control" required>
            </div>
            <button type="submit" class="btn btn-success">CONFIRM</button>
            <button type="submit" class="btn btn-danger" formaction="/admin/user-accounts" formmethod="GET">CANCEL</button>
        </form>
    </div>
</div>