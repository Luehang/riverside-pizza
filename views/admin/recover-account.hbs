<div class="row">
    <div class="col-md-8 col-md-offset-2">
        <form action="/admin/user-accounts/confirm-recover" method="POST">
            <input type="hidden" name="_csrf" value="{{ csrfToken }}">
            <h3>Recovering Account for {{userEmail}}...</h3>
            <hr>
            {{#if hasErrors}}
                <div class="alert alert-danger">
                    {{# each messages }}
                        <p>{{this}}</p>
                    {{/each}}
                </div>
            {{/if}}
            <p class="description">Please enter admin. email and password to confirm user's temporarily password or cancel.</p>
            <div class="form-group">
                <label for="email">Admin. E-Mail</label>
                <input placeholder="name@example.com" id="email" name="email" class="form-control" autocomplete="email">
            </div>
            <div class="form-group">
                <label for="password">Admin. Password</label>
                <input type="password" id="password" name="password" class="form-control">
            </div>
            <div class="form-group">
                <label for="email">User's E-Mail</label>
                <input placeholder="name@example.com" id="email" name="userEmail" class="form-control">
            </div>
            <div class="form-group">
                <label for="password">User's Temp. Password</label>
                <input type="password" id="password" name="userPassword" class="form-control">
            </div>
            <div class="form-group">
                <label for="password">User's Confirm Temp. Password</label>
                <input type="password" id="password" name="userPasswordConfirmation" class="form-control">
            </div>
            <button type="submit" class="btn btn-success">CONFIRM</button>
            <button type="submit" class="btn btn-danger" formaction="/admin/user-accounts" formmethod="GET">CANCEL</button>
        </form>
    </div>
</div>